<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup Label="Runtime">
    <PackageVersion Include="Microsoft.Extensions.Configuration" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.Binder" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.EnvironmentVariables" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.Http" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
    <PackageVersion Include="Microsoft.Extensions.Options" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup Label="Resilience">
    <!-- Brings the standard resilience handler (retry, circuit breaker, timeout)
         so the client does not hand-roll a retry loop around HttpClient. -->
    <PackageVersion Include="Microsoft.Extensions.Http.Resilience" Version="10.9.0" />
  </ItemGroup>

  <ItemGroup Label="CLI">
    <PackageVersion Include="System.CommandLine" Version="2.0.10" />
    <PackageVersion Include="Spectre.Console" Version="0.57.2" />
  </ItemGroup>

  <ItemGroup Label="Question Packs">
    <PackageVersion Include="YamlDotNet" Version="18.1.0" />
    <PackageVersion Include="JsonSchema.Net" Version="9.4.0" />
  </ItemGroup>

  <ItemGroup Label="Test">
    <!-- MTP v2 protocol for the .NET 10 SDK's `dotnet test` MTP mode. We deliberately
         do NOT centralise `xunit.v3 3.2.2` here, because that package transitively
         pulls `xunit.v3.core.mtp-v1` (the v1 protocol) which conflicts with
         `xunit.v3.mtp-v2` (which pulls `xunit.v3.core.mtp-v2`). The test projects
         reference `xunit.v3.mtp-v2` directly; that brings `xunit.v3.assert`,
         `xunit.v3.core.mtp-v2`, `xunit.v3.common`, `xunit.v3.extensibility.core`,
         and `xunit.analyzers` — all the v2 components, none of the v1.
         xunit v3 4.x would default to v2 and this package would no longer be
         needed. See ADR 0005. -->
    <PackageVersion Include="xunit.v3.mtp-v2" Version="3.2.2" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="Shouldly" Version="4.3.0" />
    <PackageVersion Include="NSubstitute" Version="6.2.0" />
    <PackageVersion Include="Spectre.Console.Testing" Version="0.57.2" />
    <PackageVersion Include="Verify.XunitV3" Version="31.28.0" />
    <PackageVersion Include="WireMock.Net" Version="2.14.0" />
    <!-- FakeTimeProvider. Polling is the component most worth testing and the one
         most tempting to test with real delays; a fake clock keeps the suite fast
         and makes timeout assertions exact rather than flaky. -->
    <PackageVersion Include="Microsoft.Extensions.TimeProvider.Testing" Version="10.9.0" />
  </ItemGroup>

</Project>
